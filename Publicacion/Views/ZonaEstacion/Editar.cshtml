@model SistemaExperto.Models.ZonaEstacion

@{
    ViewBag.Title = "Editar";
}

<h2>Editar</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ZonaEstacion</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ZonaEstacionId)

        <div class="form-group">
            @Html.LabelFor(model => model.ZonaId, "ZonaId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("ZonaId", String.Empty)
                @Html.ValidationMessageFor(model => model.ZonaId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.EstacionId, "EstacionId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("EstacionId", String.Empty)
                @Html.ValidationMessageFor(model => model.EstacionId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Porcentaje, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Porcentaje)
                @Html.ValidationMessageFor(model => model.Porcentaje)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Volver", "Index")
</div>